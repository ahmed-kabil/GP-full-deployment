server {
    listen 80 ;
    root /app ;
    index index.html ;

location ~* \.(js|css|jpg|svg)$ {
  expires 1d ;
  add_header Cache-Control "public";
  access_log off;
}

location / {
try_files $uri $uri/ /index.html =404 ;
}
}